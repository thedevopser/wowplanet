{% extends 'base.html.twig' %}

{% block title %}Recherche de Monnaies - WowPlanet{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-12">
    <div class="max-w-6xl mx-auto">
        <div class="text-center mb-8">
            <h1 class="text-4xl md:text-5xl font-bold mb-4 text-yellow-500">
                WoW Currency Tracker
            </h1>
            <p class="text-lg" style="color: var(--text-secondary);">
                Trouvez quel personnage possède le plus de monnaie
            </p>
        </div>

        {% for message in app.flashes('success') %}
            <div class="mb-4 p-4 rounded-lg bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200">
                {{ message }}
            </div>
        {% endfor %}

        {% for message in app.flashes('error') %}
            <div class="mb-4 p-4 rounded-lg bg-red-100 dark:bg-red-900 text-red-800 dark:text-red-200">
                {{ message }}
            </div>
        {% endfor %}

        {% if not has_data %}
            <!-- Upload Form -->
            <div class="card-glass rounded-2xl p-8 md:p-10 shadow-glow max-w-2xl mx-auto mb-8" data-controller="currency-upload">
                <h2 class="text-2xl font-bold mb-4">Charger les données de monnaie</h2>
                <form action="{{ path('app_currency_upload') }}" method="POST" enctype="multipart/form-data" data-action="submit->currency-upload#submit" data-currency-upload-target="form">
                    <div class="mb-6">
                        <label for="currency_file" class="block text-sm font-medium mb-3" style="color: var(--text-secondary);">
                            Fichier JSON des monnaies
                        </label>
                        <input
                            type="file"
                            name="currency_file"
                            id="currency_file"
                            accept=".json"
                            required
                            class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-800 focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition-all text-base"
                        >
                    </div>

                    <button
                        type="submit"
                        class="w-full px-6 py-2.5 rounded-lg font-semibold btn-gradient text-white shadow-md hover:shadow-lg hover:scale-[1.02] transition-all"
                    >
                        Charger les données
                    </button>
                </form>

                <div class="mt-8 pt-6 border-t border-gray-200 dark:border-gray-700">
                    <p class="text-sm mb-3" style="color: var(--text-secondary);">
                        Format attendu du fichier JSON :
                    </p>
                    <pre class="text-xs bg-gray-100 dark:bg-gray-800 p-4 rounded overflow-x-auto"><code>{
  "characters": [
    {
      "name": "Prøtactinium",
      "faction": "Alliance",
      "lastUpdate": 1766745355,
      "currencies": [
        { "name": "Pierre de vol", "quantity": 1500 },
        { "name": "Deniers", "quantity": 2625 }
      ]
    }
  ]
}</code></pre>
                </div>

                <!-- Animated Loader -->
                <div class="hidden card-glass rounded-2xl p-8 md:p-10 shadow-glow max-w-2xl mx-auto" data-currency-upload-target="loader">
                    <div class="text-center">
                        <div class="relative inline-block mb-6">
                            <!-- Outer spinning ring -->
                            <div class="absolute inset-0 rounded-full border-4 border-transparent border-t-yellow-500 border-r-amber-500 animate-spin w-24 h-24"></div>

                            <!-- Inner counter-rotating ring -->
                            <div class="absolute inset-0 m-2 rounded-full border-4 border-transparent border-b-amber-500 border-l-yellow-500 animate-spin-reverse w-20 h-20"></div>

                            <!-- Center icon -->
                            <div class="relative flex items-center justify-center w-24 h-24">
                                <span class="text-4xl animate-pulse">💰</span>
                            </div>
                        </div>

                        <h2 class="text-2xl font-bold mb-3">Chargement en cours...</h2>
                        <p class="text-lg mb-4" style="color: var(--text-secondary);">
                            Traitement du fichier JSON
                        </p>

                        <!-- Bouncing dots -->
                        <div class="flex justify-center gap-2">
                            <div class="w-3 h-3 bg-yellow-500 rounded-full animate-bounce" style="animation-delay: 0ms"></div>
                            <div class="w-3 h-3 bg-amber-500 rounded-full animate-bounce" style="animation-delay: 150ms"></div>
                            <div class="w-3 h-3 bg-yellow-500 rounded-full animate-bounce" style="animation-delay: 300ms"></div>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <!-- Search Form -->
            <div class="card-glass rounded-2xl p-8 md:p-10 shadow-glow max-w-2xl mx-auto">
                <form action="{{ path('app_currency_process') }}" method="POST">
                    <div class="mb-6">
                        <label for="currency" class="block text-sm font-medium mb-3" style="color: var(--text-secondary);">
                            Sélectionnez une monnaie
                        </label>
                        <select
                            name="currency"
                            id="currency"
                            required
                            class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-800 focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition-all text-base"
                        >
                            <option value="">-- Choisir une monnaie --</option>
                            {% for currencyName in available_currencies %}
                                <option value="{{ currencyName }}">{{ currencyName }}</option>
                            {% endfor %}
                        </select>
                    </div>

                    <button
                        type="submit"
                        class="w-full px-6 py-2.5 rounded-lg font-semibold btn-gradient text-white shadow-md hover:shadow-lg hover:scale-[1.02] transition-all"
                    >
                        Rechercher
                    </button>
                </form>

                <div class="mt-6 text-center">
                    <form action="{{ path('app_currency_clear') }}" method="POST">
                        <button type="submit" class="text-sm text-yellow-500 hover:text-yellow-400">
                            Charger un nouveau fichier
                        </button>
                    </form>
                </div>
            </div>
        {% endif %}
    </div>
</div>

<style>
    @keyframes spin-reverse {
        from {
            transform: rotate(360deg);
        }
        to {
            transform: rotate(0deg);
        }
    }

    .animate-spin-reverse {
        animation: spin-reverse 1s linear infinite;
    }
</style>
{% endblock %}
